# LightStack Configuration

# Database (PostgreSQL)
POSTGRES_PASSWORD=changeme

# Security
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production
